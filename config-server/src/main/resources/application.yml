server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ucsal/spring-cloud-config-sergio.git
          default-label: main
          search-paths: config-repo
